version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: candle-blog-commerce:latest
    container_name: candle-blog-commerce-web
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
